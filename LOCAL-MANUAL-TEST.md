# 本机手动测试说明

当前本机环境使用以下地址和账号：

- Web：<http://localhost:13974>
- API：<http://localhost:19054>
- 工作区：`dev`
- 当前提交：`305e81811`
- 数据库：`multica_multica_974`
- 登录邮箱：`dev@localhost`
- 验证码：`888888`
- 密码：无，使用本地验证码登录

## 启动环境

从仓库根目录执行：

```bash
cd /Users/quboqin/magic/projects/multica
SDKROOT="$(xcrun --sdk macosx --show-sdk-path)" make up
```

检查服务状态：

```bash
make status
```

然后打开：

```text
http://localhost:13974
```

登录后确认进入 `dev` 工作区，并确认侧边栏存在：

- Issues
- Documents
- Collections

## 文档验收

进入：

```text
http://localhost:13974/dev/documents
```

### 正文编辑

1. 创建根文档，例如 `Manual Test Document`。
2. 输入多段正文、列表、代码块、Mermaid 图、公式和一个嵌入的保存视图。
3. 点击编辑器外部或按 `Tab`，等待页面显示“已保存”。
4. 刷新页面。

预期：文档仍然存在，正文完整保留，Mermaid、公式和嵌入内容可以重新显示，打开文档不会产生错误的假草稿。

### 文档树和页面移动

1. 创建一个父文档和两个子文档。
2. 将一个子文档拖到另一个父文档下。
3. 调整同级页面顺序。
4. 将页面移动回根目录。
5. 刷新页面。
6. 检查文档层级、面包屑、页面顺序和父子关系。

预期：刷新后层级和顺序保持不变，没有循环父子关系。

### 收藏和最近访问

1. 打开一个文档并加入收藏。
2. 返回文档列表，切换到收藏筛选。
3. 再打开几个文档，切换到最近访问。
4. 刷新页面。

预期：收藏筛选只显示收藏文档，最近访问顺序正确，刷新后状态仍保留。

### 并发编辑和冲突

1. 用两个浏览器窗口打开同一个文档。
2. 在窗口 A 修改正文。
3. 在窗口 B 打开开发者工具，将网络切换为 Offline。
4. 在窗口 B 修改正文，确认本地草稿仍然保留。
5. 恢复窗口 B 网络。
6. 先在窗口 A 保存，再在窗口 B 保存。

预期：窗口 B 不会静默覆盖窗口 A 的新版本；页面显示远端版本和本地草稿之间的冲突；显式合并或重试后才能保存；刷新后最终内容符合选择。

### 评审和发布

1. 保存正文后点击“提交评审”。
2. 确认状态变为 `reviewing`。
3. 点击“发布”，确认状态变为 `published`。
4. 再次修改正文并保存。
5. 刷新页面。

预期：未保存的草稿不能发布；本地工作区创建者可以发布；修改已发布文档后状态回到 `draft`；发布不会丢失正文版本。

当前版本的发布只会生成待处理的知识入库请求，知识抽取、索引和后续检索消费者不属于本轮验收范围。

## 全局搜索验收

搜索刚才创建的文档标题或正文内容：

1. 确认文档出现在独立的 Documents 分组中。
2. 打开“只看文档”过滤，确认任务结果被过滤掉。
3. 点击搜索结果。
4. 确认跳转到 `/dev/documents/<document-id>`。

## 集合验收

进入：

```text
http://localhost:13974/dev/collections
```

### 集合、字段和记录

1. 创建集合，例如 `Manual Test Collection`。
2. 添加 Text、Number、Select、Multi-select、Checkbox、Date、URL 字段。
3. 创建几条记录。
4. 在 Table 视图中修改字段值。
5. 刷新页面。

预期：字段和值都能保存；刷新后数据不丢失；不同记录之间的字段不会相互覆盖；无权限操作显示错误。

### Calendar 视图

1. 确认集合存在 Date 字段。
2. 切换到 Calendar，选择 Date 字段作为日期来源。
3. 切换月视图和周视图。
4. 将一条记录拖到另一天。
5. 刷新页面。

预期：记录出现在新的日期；月、周视图显示正确；刷新后拖动结果保留。

### Gallery 视图

1. 切换到 Gallery。
2. 配置展示字段。
3. 如果有 URL 字段，将其配置为封面字段。
4. 调整展示字段顺序。
5. 刷新页面。

预期：记录以卡片形式显示；封面 URL 正常显示；配置刷新后保留；点击卡片可打开对应记录或详情。

## Issues 回归验收

进入：

```text
http://localhost:13974/dev/issues
```

检查以下功能：

1. Table 视图中的列宽调整。
2. 排序、筛选和分组。
3. 切换 Calendar，拖动带日期的任务改期。
4. 刷新确认日期。
5. 切换 Gallery，点击任务卡片确认进入同一个任务。
6. 返回 Table，确认筛选、排序和列宽仍然正常。

预期：任务数据和集合数据互不混淆，任务视图中的编辑和刷新结果稳定。

## 验收记录

```text
[ ] 登录成功
[ ] 文档创建和刷新
[ ] Mermaid / 公式 / 嵌入视图
[ ] 文档树跨父级移动
[ ] 同级排序和根节点移动
[ ] 收藏和最近访问
[ ] 双窗口冲突处理
[ ] 提交评审和发布
[ ] 发布后重新编辑回到 draft
[ ] 全局搜索和只看文档
[ ] 集合创建
[ ] 集合字段和记录编辑
[ ] Calendar 改期并刷新
[ ] Gallery 配置并刷新
[ ] Issues Calendar
[ ] Issues Gallery
[ ] Issues Table 筛选、排序、分组、列宽
```

## 停止和清理

停止服务但保留数据库：

```bash
make down
```

彻底删除当前本地环境及数据库：

```bash
make destroy
```
