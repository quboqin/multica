# 本机手动测试说明

当前本机环境使用以下地址和账号：

- Web：<http://localhost:13974>
- API：<http://localhost:19054>
- 工作区：`dev`
- 当前提交：`305e81811`
- 数据库：`multica_multica_974`
- 登录邮箱：`dev@localhost`
- 验证码：`888888`
- 密码：无，使用本地验证码登录

## 启动环境

从仓库根目录执行：

```bash
cd /Users/quboqin/magic/projects/multica
SDKROOT="$(xcrun --sdk macosx --show-sdk-path)" make up
```

检查服务状态：

```bash
make status
```

然后打开：

```text
http://localhost:13974
```

登录后确认进入 `dev` 工作区，并确认侧边栏存在：

- Issues
- Documents
- Collections

## 文档验收

进入：

```text
http://localhost:13974/dev/documents
```

### 正文编辑

1. 创建根文档，例如 `Manual Test Document`。
2. 输入多段正文、列表、代码块、Mermaid 图、公式和一个嵌入的保存视图。
3. 点击编辑器外部或按 `Tab`，等待页面显示“已保存”。
4. 刷新页面。

预期：文档仍然存在，正文完整保留，Mermaid、公式和嵌入内容可以重新显示，打开文档不会产生错误的假草稿。

### 文档树和页面移动

1. 创建一个父文档和两个子文档。
2. 将一个子文档拖到另一个父文档下。
3. 调整同级页面顺序。
4. 将页面移动回根目录。
5. 刷新页面。
6. 检查文档层级、面包屑、页面顺序和父子关系。

预期：刷新后层级和顺序保持不变，没有循环父子关系。

### 收藏和最近访问

1. 打开一个文档并加入收藏。
2. 返回文档列表，切换到收藏筛选。
3. 再打开几个文档，切换到最近访问。
4. 刷新页面。

预期：收藏筛选只显示收藏文档，最近访问顺序正确，刷新后状态仍保留。

### 并发编辑和冲突

1. 用两个浏览器窗口打开同一个文档。
2. 在窗口 A 修改正文。
3. 在窗口 B 打开开发者工具，将网络切换为 Offline。
4. 在窗口 B 修改正文，确认本地草稿仍然保留。
5. 恢复窗口 B 网络。
6. 先在窗口 A 保存，再在窗口 B 保存。

预期：窗口 B 不会静默覆盖窗口 A 的新版本；页面显示远端版本和本地草稿之间的冲突；显式合并或重试后才能保存；刷新后最终内容符合选择。

### 评审和发布

1. 保存正文后点击“提交评审”。
2. 确认状态变为 `reviewing`。
3. 点击“发布”，确认状态变为 `published`。
4. 再次修改正文并保存。
5. 刷新页面。

预期：未保存的草稿不能发布；本地工作区创建者可以发布；修改已发布文档后状态回到 `draft`；发布不会丢失正文版本。

当前版本的发布只会生成待处理的知识入库请求，知识抽取、索引和后续检索消费者不属于本轮验收范围。

## 全局搜索验收

搜索刚才创建的文档标题或正文内容：

1. 确认文档出现在独立的 Documents 分组中。
2. 打开“只看文档”过滤，确认任务结果被过滤掉。
3. 点击搜索结果。
4. 确认跳转到 `/dev/documents/<document-id>`。

## 集合验收

进入：

```text
http://localhost:13974/dev/collections
```

界面对照 `apps/docs/handbook/cortex-ui-prototype.html` 的 S4（表格）、S5（布局）和 S6（行详情）。左侧是文档与表格共用的导航栏，表格列表显示记录数，`+` 可以新建表格。

### 集合、字段和记录

1. 在左侧“表格”旁点击 `+` 创建集合，例如 `Manual Test Collection`。点击面包屑里的表名可以重命名。
2. 点击表头最右侧 `+`，依次添加 Text、Number、Select、Multi-select、Checkbox、Date、URL、Member 字段；Select 类字段在同一弹窗里填写选项和颜色。
3. 在表格底部“新建行”输入名称后回车，连续创建几条记录。
4. 直接在单元格里编辑：Select 单选后关闭；Multi-select 在弹出列表里连续勾选多个选项，列表保持打开；Date 用日历；Checkbox 直接勾选。
5. 刷新页面。

预期：字段和值都能保存；Multi-select 的多个选项都保留，不出现冲突提示；刷新后数据不丢失；不同记录之间的字段不会相互覆盖。

### 字段菜单与选项编辑

1. 点击 Select 字段的表头，菜单应显示：字段类型、编辑选项、升序/降序、按此字段分组、按此字段过滤、在本视图隐藏、归档字段，以及底部“本表字段 N / 50”配额。
2. 打开“编辑选项”：改一个选项名、改颜色、新增一个选项、删除一个已被使用的选项。删除时弹窗会提示哪些选项会从记录中清除。
3. 保存后刷新。
4. 在“字段类型”里把 Select 改成 Multi-select，再把 Number 改成 Text；确认 Text 不能改成 URL 或 Date。
5. 归档一个字段。

预期：改名和颜色立即反映到所有单元格；被删除的选项从记录中清除，不显示原始 id；类型只能按安全方向转换且值保留；归档后字段从表中消失，数据仍在数据库中。非表格创建者、非 owner/admin 看不到字段管理项。

### 视图栏：过滤、分组、排序、显示和保存视图

1. 用“过滤”添加条件（数字大于、文本包含、选项等于），用“排序”按某个字段升/降序。
2. 用“分组”按 Select 字段分组，确认每组显示数量，每组底部“新建行”会带上该组的值。
3. 用“显示”隐藏若干列。
4. 点击视图标签旁的 `+` 保存为视图，切换“全部记录”和新视图；修改新视图后出现“保存到视图 / 重置”。

预期：过滤、排序和分组由服务端完成，刷新后保留；保存的视图恢复同样的布局、过滤、排序和列。

### 看板、日历和画廊

1. 用布局切换切到看板：按 Select 字段分列，被过滤清空的列以虚线占位保留；把卡片拖到另一列，点击列头 `+` 在该列新建记录。
2. 切到日历：用“日期字段”选择日期来源，切换月/周和“今天”，把记录拖到另一天。
3. 切到画廊：在“显示”里选择封面 URL 字段和卡片字段。
4. 刷新页面。

预期：拖动结果写回字段并在刷新后保留；没有封面时退化为文字卡片；点击卡片打开行详情。

### 行详情和回收站

1. 鼠标移到行名称上点击“打开”，右侧出现行详情：可以改标题和所有字段，并显示“这一行没有评论区”的说明。
2. 在行详情 `···` 或在名称上右键选择“移到回收站”。
3. 点击底部“回收站 N（保留 30 天）”，恢复这条记录。

预期：删除后记录从表格消失并出现在回收站；恢复后回到表格。

### 冲突

1. 用两个窗口打开同一集合，窗口 A 修改某个 Text 单元格。
2. 窗口 B 不刷新，修改同一单元格并回车。

预期：窗口 B 提示“保存前这一格已被他人修改”，显示当前值，并提供“用我的值覆盖”；不点击时不会覆盖窗口 A 的值。

## Issues 回归验收

进入：

```text
http://localhost:13974/dev/issues
```

检查以下功能：

1. Table 视图中的列宽调整。
2. 排序、筛选和分组。
3. 切换 Calendar，拖动带日期的任务改期。
4. 刷新确认日期。
5. 切换 Gallery，点击任务卡片确认进入同一个任务。
6. 返回 Table，确认筛选、排序和列宽仍然正常。

预期：任务数据和集合数据互不混淆，任务视图中的编辑和刷新结果稳定。

## 验收记录

```text
[ ] 登录成功
[ ] 文档创建和刷新
[ ] Mermaid / 公式 / 嵌入视图
[ ] 文档树跨父级移动
[ ] 同级排序和根节点移动
[ ] 收藏和最近访问
[ ] 双窗口冲突处理
[ ] 提交评审和发布
[ ] 发布后重新编辑回到 draft
[ ] 全局搜索和只看文档
[ ] 集合创建
[ ] 集合字段和记录编辑
[ ] Calendar 改期并刷新
[ ] Gallery 配置并刷新
[ ] Issues Calendar
[ ] Issues Gallery
[ ] Issues Table 筛选、排序、分组、列宽
```

## 停止和清理

停止服务但保留数据库：

```bash
make down
```

彻底删除当前本地环境及数据库：

```bash
make destroy
```
