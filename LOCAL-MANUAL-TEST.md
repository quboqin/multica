# 本机手动测试说明

当前本机环境使用以下地址和账号（端口与数据库名来自仓库根目录的 `.env`，以 `make status` 的输出为准）：

- Web：<http://localhost:13703>
- API：<http://localhost:18783>
- 工作区：`dev`
- 数据库：`multica_multica_703`
- 登录邮箱：`dev@localhost`
- 验证码：`888888`
- 密码：无，使用本地验证码登录

## 启动环境

从仓库根目录执行：

```bash
cd /Users/quboqin/magic/projects/multica
SDKROOT="$(xcrun --sdk macosx --show-sdk-path)" make up
```

检查服务状态：

```bash
make status
```

然后打开：

```text
http://localhost:13703
```

登录后确认进入 `dev` 工作区，并确认侧边栏存在：

- Issues
- Documents
- Collections

## 文档验收

进入：

```text
http://localhost:13703/dev/documents
```

### 正文编辑

1. 创建根文档，例如 `Manual Test Document`。
2. 输入多段正文、列表、代码块、Mermaid 图、公式和一个嵌入的保存视图。
3. 点击编辑器外部或按 `Tab`，等待页面显示“已保存”。
4. 刷新页面。

预期：文档仍然存在，正文完整保留，Mermaid、公式和嵌入内容可以重新显示，打开文档不会产生错误的假草稿。

### 文档树和页面移动

1. 创建一个父文档和两个子文档。
2. 将一个子文档拖到另一个父文档下。
3. 调整同级页面顺序。
4. 将页面移动回根目录。
5. 刷新页面。
6. 检查文档层级、面包屑、页面顺序和父子关系。

预期：刷新后层级和顺序保持不变，没有循环父子关系。

### 收藏和最近访问

1. 打开一个文档并加入收藏。
2. 返回文档列表，切换到收藏筛选。
3. 再打开几个文档，切换到最近访问。
4. 刷新页面。

预期：收藏筛选只显示收藏文档，最近访问顺序正确，刷新后状态仍保留。

### 并发编辑和冲突

1. 用两个浏览器窗口打开同一个文档。
2. 在窗口 A 修改正文。
3. 在窗口 B 打开开发者工具，将网络切换为 Offline。
4. 在窗口 B 修改正文，确认本地草稿仍然保留。
5. 恢复窗口 B 网络。
6. 先在窗口 A 保存，再在窗口 B 保存。

预期：窗口 B 不会静默覆盖窗口 A 的新版本；页面显示远端版本和本地草稿之间的冲突；显式合并或重试后才能保存；刷新后最终内容符合选择。

### 评审和发布

1. 保存正文后点击“提交评审”。
2. 确认状态变为 `reviewing`。
3. 点击“发布”，确认状态变为 `published`。
4. 再次修改正文并保存。
5. 刷新页面。

预期：未保存的草稿不能发布；本地工作区创建者可以发布；修改已发布文档后状态回到 `draft`；发布不会丢失正文版本。

当前版本的发布只会生成待处理的知识入库请求，知识抽取、索引和后续检索消费者不属于本轮验收范围。

## 全局搜索验收

搜索刚才创建的文档标题或正文内容：

1. 确认文档出现在独立的 Documents 分组中。
2. 打开“只看文档”过滤，确认任务结果被过滤掉。
3. 点击搜索结果。
4. 确认跳转到 `/dev/documents/<document-id>`。

## 集合验收

进入：

```text
http://localhost:13703/dev/collections
```

界面对照 `apps/docs/handbook/cortex-ui-prototype.html` 的 S4（表格）、S5（布局）和 S6（行详情）。点击侧栏的 Collections 后，侧栏右侧新增一列表格列表：可按名称搜索，显示记录数，`+` 可以新建表格。这一列只有表格；文档树在 Documents 自己的一列里。

### 集合、字段和记录

1. 在第二列“表格”旁点击 `+` 创建集合，例如 `Manual Test Collection`。点击面包屑里的表名可以重命名。
2. 新建的表只有“名称”一列，紧挨着它的表头格写着 `+ 新建字段`（有字段后缩成 `+`，始终跟在最后一个字段后面）。点击后面板出现在这一格正下方：填名称、选字段类型（类型列表从面板侧面展开），按回车或“创建字段”。依次添加 Text、Number、Select、Multi-select、Checkbox、Date、URL、Member 字段；Select 类字段在同一个面板里填写选项和颜色。
3. 在表格底部“新建行”输入名称后回车，连续创建几条记录。
4. 直接在单元格里编辑：Select 单选后关闭；Multi-select 在弹出列表里连续勾选多个选项，列表保持打开；Date 用日历；Checkbox 直接勾选。
5. 刷新页面。

预期：字段和值都能保存；Multi-select 的多个选项都保留，不出现冲突提示；刷新后数据不丢失；不同记录之间的字段不会相互覆盖。

### 字段菜单与选项编辑

1. 点击 Select 字段的表头，菜单应显示：修改字段（右侧带类型和选项数）、升序/降序、按此字段分组、按此字段过滤、在本视图隐藏、归档字段，以及底部“本表字段 N / 50”配额。
2. 点“修改字段”，面板出现在这一列表头正下方：改字段名、改一个选项名、改颜色、新增一个选项、删除一个已被使用的选项。删除时面板会提示哪些选项会从记录中清除。
3. 保存后刷新。
4. 在面板的“字段类型”里把 Select 改成 Multi-select，再把 Number 改成 Text；确认 Text 不能改成 URL 或 Date。重名字段会在面板里显示服务端错误，面板不关闭。
5. 归档一个字段。
6. 点击首列“名称”表头格的任意位置（不必点在文字上；悬停时整格变色、右端出现下拉箭头）：菜单只有修改字段（右侧标“文本”）、升序、降序。点“修改字段”，面板出现在首列正下方，输入框里是当前列名，字段类型不可选并写明原因；改成“客户”保存，刷新。再打开一次，把名称清空（或改回“名称”）保存。

预期：改名和颜色立即反映到所有单元格；被删除的选项从记录中清除，不显示原始 id；类型只能按安全方向转换且值保留；归档后字段从表中消失，数据仍在数据库中。首列改名后表头和“排序”里都显示新名字，刷新后保留；清空后回到随界面语言变化的默认名。首列没有隐藏、归档、分组。非表格创建者、非 owner/admin 看不到字段管理项。

### 视图栏：过滤、分组、排序、显示和保存视图

1. 用“过滤”添加条件（数字大于、文本包含、选项等于），用“排序”按某个字段升/降序。
2. 用“分组”按 Select 字段分组，确认每组显示数量，每组底部“新建行”会带上该组的值。
3. 用“显示”隐藏若干列。
4. 点击视图标签旁的 `+` 保存为视图，切换“全部记录”和新视图；修改新视图后出现“保存到视图 / 重置”。

预期：过滤、排序和分组由服务端完成，刷新后保留；保存的视图恢复同样的布局、过滤、排序和列。

### 看板、日历和画廊

1. 用布局切换切到看板：按 Select 字段分列，被过滤清空的列以虚线占位保留；把卡片拖到另一列，点击列头 `+` 在该列新建记录。
2. 切到日历：用“日期字段”选择日期来源，切换月/周和“今天”，把记录拖到另一天。
3. 切到画廊：在“显示”里选择封面 URL 字段和卡片字段。
4. 刷新页面。
5. 新建一张表，不加字段就切到看板：提示下方有“新建字段”，点开后字段类型已是单选；创建后看板出现。再切到日历：同样的按钮，类型已是日期。
6. 在表格、看板或画廊里打开“显示”：第一行是首列（始终显示，不可勾选），它和每个字段右侧的铅笔都打开修改面板，底部“新建字段”打开新建面板，面板都出现在“显示”按钮下方。日历里这个入口叫“字段”，只有列表没有勾选。

预期：拖动结果写回字段并在刷新后保留；没有封面时退化为文字卡片；点击卡片打开行详情。布局是本浏览器记住的偏好，停在看板、日历或画廊的表下次打开仍是该布局，此时照样能添加、修改字段；非管理者看不到这些入口。

### 在列表里删除文档和表格

1. 在 Documents 的第二列里，鼠标移到一篇有子页的文档上：行尾出现收藏、添加子页和“···”，标题相应缩短；移开后恢复。用 Tab 键聚焦到这一行时同样出现。
2. 点“···”→“删除文档”。确认框标题带文档名，正文说明子页会移到根层级。先点“取消”，再重复一次并点“删除”。
3. 在 Collections 的第二列里，鼠标移到自己创建的表上：记录数让位给“···”。点“···”→“删除表格”，确认。
4. 删除当前正打开的那篇文档 / 那张表各一次。

预期：确认前不发请求；确认后按钮变为“删除中…”，服务端成功后该行消失并提示已删除。被删文档的子页出现在根层级。删除正打开的一项后回到 Documents / Collections 首页。表格删除后不会再出现在活动列表里；勾选“已归档表格”后，管理者可以恢复，记录和字段随表重新可见。普通成员在别人创建的表上看不到“···”，记录数保持显示；owner / admin 可以删除任何表。

### Excel / CSV 自动创建多维表格

1. 打开 Collections，在表格列表点击“导入 Excel / CSV”。选择 `.xlsx` 文件（或 UTF-8 CSV），无需下载模板，也无需先建空表。
2. 多工作表文件选择要导入的一张；第一行不是表头时修改“表头所在行”。预览显示行数、字段、识别类型和示例值。
3. 确认名称列，按需修改表名、项目、字段名称和类型。取消勾选可以跳过列；最多 50 个字段加名称列。
4. 点击“创建表格”。预期新表出现在 Collections，自动打开；刷新后字段和记录仍在。带前导零的编号和超过 15 位的数字保留为文本。
5. 可以用仓库里的 `e2e/fixtures/cortex-customers.xlsx` 测试：首张 Sheet1 为空，选择 Customers 后应有 3 行；日期、勾选、数字、网址正确识别，Code 保留 `000123`。把 Stage 改为单选，取消 Ignore 列，再创建。
6. 若类型转换报错，表尚未创建，预览和选择保留；改回文本后可重试。取消预览不会创建任何表或记录。

支持 `.xlsx` 和 UTF-8 `.csv`，最多 24 MiB、1 万行；旧 `.xls` 需先另存为 `.xlsx`。每次选择一张工作表创建一个新表。公式使用已保存的结果，没有缓存结果时保留公式文本；排版、图表和公式计算关系不迁移。识别和可选字段类型为文本、数字、日期、勾选、网址、单选；成员与关联可在导入后配置。

表内原有入口已命名为“追加 CSV 记录”，用于给当前表追加记录；它与“从文件创建新表”是两种操作。

### P2a：项目建表、CSV 与批量操作（2026-09-21）

本轮在 `codex/cortex-g1-ui-rework` 上实现；自动化通过不代表人工验收完成。详细验证记录见 [cortex-p2a-development.md](apps/docs/handbook/cortex-p2a-development.md)。

1. 打开项目详情，在资源区域点击“新建表格”。确认项目已经选中，填写名称、图标和描述后创建；刷新后仍属于该项目，表格列表显示图标。
2. 打开“表格设置”，修改图标和描述并保存。归档一个有值的字段，再从设置中的“已归档字段”恢复；确认原值回来。若活动字段重名，先改名再恢复；50 个活动字段时必须先腾出名额。
3. 点击“追加 CSV 记录”，可按需下载表头模板。文件使用 UTF-8，第一列 `title`，其余列使用已有字段名称或 ID。先“校验”，确认行数后导入。多值单元格使用 JSON 数组，选项可用名称，成员使用 `member:用户ID`；空格保留，空单元格不写值。关联通过“关联”操作另行写入。
4. 用一万行文件导入，刷新检查行数和字段。把最后一行的数字改成无效文本再导入：预期显示行与字段错误，整份文件不写入。导入是新增记录，重复导入会新增另一批。
5. 筛选后点击“选择最多 500 条匹配记录”，或勾选单行/已加载行。选择一个字段和新值，点击“应用到所选”；空值会清空该字段。预期只修改选中行，其余字段和值保留。
6. 再选择 500 行，点击“移到回收站”。取消时没有删除；确认后回收站增加 500 行。进入回收站并恢复显示的记录（每页最多 200），重复至全部恢复。
7. 两个窗口打开同一张表。在窗口 A 选中记录，窗口 B 修改其中一行，再在 A 批量修改：预期冲突提示，整批未写入。清除选择并重新选择后才能基于最新版本重试。
8. 从列表归档该表，勾选“已归档表格”，点击恢复。确认记录、字段、图标、描述和项目归属仍然保留。

CLI 验收（先 `make build`，使用已登录且指向本地环境的 `server/bin/multica`）：

```bash
server/bin/multica record import '验收表' --file /tmp/cortex-p2a-import.csv --dry-run
server/bin/multica record import '验收表' --file /tmp/cortex-p2a-import.csv
server/bin/multica record list '验收表' --filter '分数>=10' --sort 分数
server/bin/multica record batch-update '验收表' --record '<行ID>' --set '分数=42' --expect-revision '<行ID>=<读取到的版本>'
server/bin/multica record batch-delete '验收表' --record '<行ID>' --yes
server/bin/multica record batch-restore '验收表' --record '<行ID>'
server/bin/multica collection list --archived
server/bin/multica collection restore '验收表'
server/bin/multica collection field list '验收表' --archived
server/bin/multica collection field restore '验收表' '备注'
```

批量命令可重复传 `--record`，每次 1–500 行。传预期版本时必须覆盖全部选中行。智能体仍按运行时主人的表结构权限操作；本轮没有执行真实智能体运行。

### 行详情和回收站

1. 鼠标移到行名称上点击“打开”，右侧出现行详情：可以改标题和所有字段，并显示“这一行没有评论区”的说明。
2. 在行详情 `···` 或在名称上右键选择“移到回收站”。
3. 点击底部“回收站 N（保留 30 天）”，恢复这条记录。

预期：删除后记录从表格消失并出现在回收站；恢复后回到表格。

### 关联字段、转为任务和反向引用（FR-028）

先准备：一张“需求池”表、一张“客户名单”表（各有几条记录），以及两三个任务。以下界面对照原型 S4 的“关联字段”两屏和 S6。

1. 在“需求池”表头点 `+`：字段类型列表末尾多了“关联”。选它后面板多出“关联到”，默认“任务”，下拉里还有本工作区的每张表；下方写着“关联对象创建后不能更改”。建一个关联到任务的字段“关联任务”，再建一个关联到“客户名单”的字段“提出客户”。
2. 点某一行的“关联任务”格：弹出搜索框，提示输入关键词。输入任务标题的一部分，点结果里的任务（或直接回车选第一条）；列表不关闭，再搜一个、再点一个。搜索时列表只显示匹配项，已关联的带勾。按 Esc 关闭。
3. 点同一行的“提出客户”格：不用输入就列出“客户名单”的记录，点一条。
4. 再点开“关联任务”格，在“已关联”里点其中一条取消关联。
5. 打开这一行的行详情：两个关联字段各占一节，任务那节每条是“状态图标 + 编号 + 标题”，点击进入任务；悬停时右侧出现取消关联；每节底部是“关联已有任务 / 关联记录”按钮（同一个搜索框）。
6. 打开另一行的行详情，点红色说明里的“转为任务”：对话框里标题预填行标题，可以改；点“创建任务”。
7. 从行详情点进刚创建的任务：右侧栏出现“关联记录”一节，列着“需求池 + 行标题”；点它。
8. 回到任务，从右上角 `···` 选“删除任务”，看确认框；确认删除后回到“需求池”。
9. 表头菜单、“排序”和“过滤”里找关联字段。
10. （可选）用一个不是表格创建者的普通成员，在一张没有“关联到任务”字段的表里打开行详情，点“转为任务”。

预期：
- 第 2～4 步：格子里任务显示为蓝色 chip（带任务当前状态的图标和编号，悬停看标题），记录显示为灰色 chip；刷新后保留。重复关联同一目标不会出现两条。
- 第 6 步：提示“已创建 XXX-N 并关联到这条记录”，带“打开”；原行还在，行详情的“关联任务”里多了这条任务。如果表里原先没有关联到任务的字段，对话框会先说明“表格会新增「关联任务」字段”，创建后表里多出这一列。任务建在表格所属的项目里。
- 第 7 步：地址变为 `…/collections/<表>?record=<行>`，表格打开且该行的详情已展开。没有任何记录指向的任务不显示“关联记录”这一节。
- 第 8 步：确认框多一行“有 N 条表格记录关联了该任务，删除后这些关联会显示为「已删除」”。删除后，表里那一格是灰色删除线的“已删除”，仍可在搜索框的“已关联”里点掉。
- 第 9 步：关联字段的表头菜单只有修改字段、在本视图隐藏、归档字段；“排序”和“过滤”的字段列表里没有它。修改字段时类型和“关联到”都不可改，只能改名。
- 第 10 步：“创建任务”不可点，并说明需要表格创建者或工作区管理员先添加字段。
- 把被关联的客户记录移到回收站：那一格显示“已删除”；从回收站恢复后重新显示客户名。被其他记录关联的行，行详情里会出现“被以下记录引用”。

### 冲突

1. 用两个窗口打开同一集合，窗口 A 修改某个 Text 单元格。
2. 窗口 B 不刷新，修改同一单元格并回车。

预期：窗口 B 提示“保存前这一格已被他人修改”，显示当前值，并提供“用我的值覆盖”；不点击时不会覆盖窗口 A 的值。

## 命令行与智能体验收（FR-027）

文档和表格现在可以用 `multica` 命令行操作，智能体走的也是这一套命令。先用你自己的身份走一遍，再让一个真实的智能体做同样的事，看它被允许和被拒绝的地方。

准备：

```bash
# 在仓库根目录执行
make build                                  # 生成 server/bin/multica
export PATH="$PWD/server/bin:$PATH"
export MULTICA_SERVER_URL="http://localhost:$(grep -E '^PORT=' .env | cut -d= -f2)"   # 本机是 18783；`make status` 的 Backend 一行也会打印
export MULTICA_TOKEN=<在 Web「设置 → API Token」里新建的 mul_ 令牌>
multica workspace list --full-id            # 记下 dev 工作区的完整 ID
export MULTICA_WORKSPACE_ID=<上一步的 ID>
```

令牌只放在当前终端的环境变量里，不要写进仓库里的任何文件。

### 表格

沿用上一节的“需求池”“客户名单”两张表（表名按你实际建的来）。

1. `multica collection list`，再 `multica collection get 需求池 --output table`：看到每个字段的名称、类型、选项，关联字段写着 `issues` 或 `table 客户名单`。
2. `multica record create 需求池 --title "命令行建的行" --set "状态=待评估"`（字段名、选项名换成你表里的）。回到浏览器，不刷新也能看到这一行出现。
3. `multica record list 需求池 --filter "状态=待评估"`，再加 `--output json` 看一眼：单元格在 `values` 下，键是字段名，值是选项名而不是 ID。
4. `multica record update 需求池 "命令行建的行" --set "状态=已排期" --expect "状态=待评估"` 成功；**原样再执行一次**，这次应当失败。
5. `multica record link 需求池 "命令行建的行" --field "关联任务" --to <某个任务编号>`，然后 `multica issue records <同一个任务编号>`。
6. 故意写错：`--set "状态=不存在的选项"`、`--set "关联任务=XXX-1"`、`multica record list 需求池 --filter "关联任务=XXX-1"`。
7. `multica record delete 需求池 "命令行建的行"` → `multica record trash 需求池` → `multica record restore 需求池 "命令行建的行"`。
8. 表结构：`multica collection create --name "命令行建的表"`，浏览器不刷新，左侧表格列表里出现这张表；`multica collection field add 命令行建的表 --name 阶段 --type select --option "线索" --option "成交:#22c55e"`；`multica collection field update 命令行建的表 阶段 --add-option "已流失:#ef4444"`；最后 `multica collection get 命令行建的表 --output table`。

预期：
- 第 4 步第二次执行返回“field changed; reload and retry”，浏览器里的值没有被覆盖。
- 第 8 步：`阶段` 有三个选项，前两个原样保留。`--add-option` 只追加；`--option` 是整体替换，漏写的选项会从所有行里清掉，两者同时给会直接报错。再执行一次同样的 `--add-option` 会提示该选项已存在、什么都没改。
- 第 5 步之后，浏览器里那一格出现蓝色任务 chip；`issue records` 列出“需求池 + 行标题”。
- 第 6 步三条命令都在发请求之前就报错：第一条列出可用选项，第二条提示改用 `record link`，第三条说明关联字段暂不支持过滤。

### 文档

1. `multica document list`：缩进表示层级，不含正文。
2. `multica document create --title "命令行写的文档" --content "# 第一版"`，记下编号（形如 `XXX-12`）。
3. `multica document get XXX-12 --output markdown > /tmp/doc.md`，终端里另外打印了 `revision 1`。改一下 `/tmp/doc.md`，然后 `multica document save XXX-12 --expected-revision 1 --content-file /tmp/doc.md --allow-external-file`。
4. **不改版本号再保存一次**（仍然 `--expected-revision 1`）。
5. 在浏览器里打开这篇文档改几个字并等“已保存”，再用命令行按旧版本保存。
6. `multica document move XXX-12 --parent <另一篇文档的编号>`，浏览器里的文档树跟着变；再 `--root --first` 移回顶层第一位。
7. `multica document status XXX-12 reviewing --expected-revision <当前版本>`，再 `published`。
8. `multica issue search "第一版" --kind doc`。

预期：
- 第 4、5 步都被拒绝，提示形如“nothing was written: XXX-12 is at revision 2, and this command named revision 1 …”，并要求先重新读取、合并，再用新版本保存；文档内容不变。
- 第 7 步：你是工作区 owner / admin 时两步都成功；发布后再用命令行改正文，状态回到 draft。
- 不带 `--expected-revision` 的 `save` 直接报错，不发请求。

### 让智能体来做（可选，需要本机有可用的智能体运行时）

1. 重启守护进程让它用上新编译的 CLI：`make daemon`。
2. 新建一个任务指派给智能体，内容类似：“读取表格『需求池』，把标题含『命令行』的那一行的状态改成『已上线』；给『需求池』加一个文本字段『备注』，再给『状态』加一个选项『已搁置』；新建一张表『客户名单（智能体）』，带单选字段『阶段』（线索、成交），写两行；再新建一篇文档，标题『智能体周报』，写三行摘要，并把它提交评审。把每一步的结果写在评论里。”
3. 等它跑完，看评论、表格和文档树。

预期：
- 行的状态被改掉、文档建出来了，评论里写明这两件事成功。
- 表结构也改成了：『需求池』多了『备注』列，『状态』多了『已搁置』且原有选项和各行的值都还在；新表不用刷新就出现在表格列表里，里面有两行。智能体的任务令牌按**运行时主人**的身份判定——运行时是你的，所以新表的创建者是你，你在界面上能继续改它的字段；如果运行时主人既不是某张表的创建者、也不是工作区 owner / admin，改那张表的结构会被拒（“only a table's creator or a workspace owner/admin can change its structure …”），写行不受影响。
- 提交评审**失败**，智能体在评论里转述了原因（“… approvals signed by a person …”），没有去尝试别的办法；文档的状态没变。
- 这次运行里的评论作者是智能体；`multica issue runs <任务编号>` 能看到这次运行。

## Issues 回归验收

进入：

```text
http://localhost:13703/dev/issues
```

检查以下功能：

1. Table 视图中的列宽调整。
2. 排序、筛选和分组。
3. 切换 Calendar，拖动带日期的任务改期。
4. 刷新确认日期。
5. 切换 Gallery，点击任务卡片确认进入同一个任务。
6. 返回 Table，确认筛选、排序和列宽仍然正常。

预期：任务数据和集合数据互不混淆，任务视图中的编辑和刷新结果稳定。

## 验收记录

```text
[ ] 登录成功
[ ] 文档创建和刷新
[ ] Mermaid / 公式 / 嵌入视图
[ ] 文档树跨父级移动
[ ] 同级排序和根节点移动
[ ] 收藏和最近访问
[ ] 双窗口冲突处理
[ ] 提交评审和发布
[ ] 发布后重新编辑回到 draft
[ ] 全局搜索和只看文档
[ ] 集合创建
[ ] P2a：项目建表、图标和描述
[ ] P2a：一万行 CSV、错误整份不写入
[ ] P2a：500 行批量修改、冲突整批回滚、删除及恢复
[ ] P2a：归档表和字段恢复，原值保留
[ ] 集合字段和记录编辑
[ ] 关联字段：关联任务 / 关联另一张表的记录
[ ] 转为任务，并从任务回到这一行
[ ] 删除被关联的任务：删除前提示、表里显示“已删除”
[ ] 命令行：读表、建行、按名称写格、--expect 冲突、关联与反查、回收站
[ ] 命令行：文档列表、按版本保存、旧版本保存被拒、移动、评审与发布
[ ] 命令行：建表、加字段、`--add-option` 追加选项，原有选项保留
[ ] 智能体：能建表、加字段和选项、写行和文档；提交评审被拒且说明了原因（可选）
[ ] Calendar 改期并刷新
[ ] Gallery 配置并刷新
[ ] Issues Calendar
[ ] Issues Gallery
[ ] Issues Table 筛选、排序、分组、列宽
```

## 停止和清理

停止服务但保留数据库：

```bash
make down
```

彻底删除当前本地环境及数据库：

```bash
make destroy
```

## 集合公式字段

公式功能的操作步骤、示例与限制见 [公式说明与验收](apps/docs/handbook/cortex-formulas.md)。新增字段时选择“公式”，例如 `{数量} * {单价}`；公式结果只读。

2026-09-22 自动验证：全项目 `pnpm typecheck`、`pnpm lint`（存在已有警告）、`make build` 通过；集合前端 46 项、核心集合/API 124 项、Go 公式/集合/属性/CLI 定向测试通过；Chromium 端到端验收通过。未运行全量 `make test` 和真实智能体运行。当前本地环境已加载公式代码，人工验收待确认。
