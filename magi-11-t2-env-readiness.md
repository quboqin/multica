环境就绪清单（脱敏）

结论：未就绪，因建议隔离目录已存在而按安全约束停止。未安装依赖、未创建或覆盖目录、未删除容器/数据、未启动测试服务，未执行 G0～G10。

目标

- 候选 SHA：`b1954bf42cb6a095eaea337b3ab5921312477e54`
- 目标隔离目录：`/home/openclaw/multica-test-envs/magi11-t2-b1954bf`
- 目录检查：已存在；未读取其内容，未对其做任何修改。

Runtime 与主机预检

- Ubuntu runtime：已确认在线；daemon 为 `01a042e1-4264-76f3-bf63-8208136484ac`。
- 执行用户：普通用户 `openclaw`（uid 1000）；未使用 root 或全局安装路径。
- 图形能力：Linux 图形会话相关工具可见（Chrome、Xvfb）；未启动 Desktop。
- Docker：Engine 与 Compose 可用；当前没有运行中的容器。已有一个停止状态的非本任务容器，未触碰。
- 当前工具缺口：Go、`psql`、`pg_isready` 不在 PATH；Node 当前解析为 26.x，pnpm 为 10.28.2。由于目录停止条件，未尝试补齐或切换版本。

可达性

- Multica runtime 心跳：可达且在线。
- 验收 Web/API/WS：未启动，暂无可报告的验收地址；因此不可达性/健康检查尚未验证。
- 当前监听中仅观察到 runtime daemon 及其他既有进程；未将其当作验收服务。

持久日志

- 验收服务日志：未建立（服务未启动）。
- 既有 runtime/其他进程日志：不属于本验收环境，本轮未读取、修改或停止。

停止方式

- 本轮没有由验收准备启动的进程或容器，无需停止。
- 后续若在经确认的新独立 checkout 成功登记环境，应只在该 checkout 执行 `make down`；该操作保留验收数据库。
- 不执行 `make destroy`，不删除既有目录或容器，不停止 Multica daemon。

恢复条件

由环境负责人确认一个不存在且可由普通用户创建的独立目录（或明确书面授权另一路径），并确认用户级 Node 22、Go >= 1.26.6、项目依赖、匹配 Chromium、隔离 PG17/Docker 资源、独立端口与测试身份的安装范围。确认后重新做只读预检，再继续准备；环境就绪前不启动 G0～G10，T2 未通过前不启动 T3。
